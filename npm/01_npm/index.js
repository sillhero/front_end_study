// 1、导入uniq模块
const uniq = require('uniq');

// 2、使用函数
let arr = [1, 2, 3, 4, 5, 6, 3, 2, 1];

const result = uniq(arr);

console.log(result);

const math  = require('learnmyself');

console.log(math.add(1, 2));

console.log(math.sub(1, 2));